# 🎓 Educational Platform API

Проект REST API для образовательной платформы с системой курсов, уроков, подписок и платежей.

## 🚀 Быстрый старт

### 1. Клонирование и настройка
```bash
# Создание виртуального окружения
python -m venv venv

# Активация окружения
# Windows:
venv\\Scripts\\activate
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка базы данных
```bash
# Применение миграций
python manage.py migrate

# Создание тестовых данных
python manage.py shell
# В интерактивной консоли выполните код из create_test_data.py
```

### 3. Создание суперпользователя
```bash
python manage.py createsuperuser
```

### 4. Запуск сервера
```bash
python manage.py runserver
```

Сервер будет доступен по адресу: http://localhost:8000

## 📚 API Endpoints

### Аутентификация
- `POST /api/v1/users/token/` - Получение JWT токена
- `POST /api/v1/users/token/refresh/` - Обновление токена

### Курсы
- `GET /api/v1/materials/courses/` - Список курсов
- `POST /api/v1/materials/courses/` - Создание курса (только админ)
- `GET /api/v1/materials/courses/{id}/` - Детали курса
- `PUT /api/v1/materials/courses/{id}/` - Обновление курса
- `DELETE /api/v1/materials/courses/{id}/` - Удаление курса

### Уроки
- `GET /api/v1/materials/lessons/` - Список уроков
- `POST /api/v1/materials/lessons/` - Создание урока
- `GET /api/v1/materials/lessons/{id}/` - Детали урока

### Подписки
- `POST /api/v1/materials/subscription/` - Добавить/удалить подписку

### Пользователи
- `GET /api/v1/users/profile/` - Профиль пользователя
- `GET /api/v1/users/payments/` - История платежей

## 👥 Тестовые пользователи

### Администратор
- **Email:** admin@example.com
- **Пароль:** admin123
- **Права:** Полный доступ ко всем функциям

### Модератор
- **Email:** moderator@example.com
- **Пароль:** moderator123
- **Права:** Просмотр и редактирование контента

### Обычный пользователь
- **Email:** user@example.com
- **Пароль:** user123
- **Права:** Доступ только к своим данным

## 📁 Структура проекта

```
educational_platform_project/
├── 📄 manage.py                    # Запуск Django
├── 📦 requirements.txt             # Зависимости Python
├── 📖 README.md                    # Документация
├── 📋 INSTRUCTIONS.md              # Инструкции по запуску
├── 🧪 create_test_data.py          # Создание тестовых данных
├── 🎯 final_test.py                # Финальный тест проекта
├── 📊 FINAL_REPORT.txt             # Итоговый отчет
├── 🗂️  .gitignore                  # Исключения Git
├── 📁 config/                      # Настройки Django
│   ├── settings.py                # Основные настройки
│   ├── urls.py                    # URL маршруты
│   ├── wsgi.py                    # WSGI конфигурация
│   └── asgi.py                    # ASGI конфигурация
├── 📁 materials/                   # Приложение материалов
│   ├── models.py                  # Модели: Course, Lesson, Subscription
│   ├── views.py                   # Представления API
│   ├── serializers.py             # Сериализаторы
│   ├── urls.py                    # URL маршруты
│   └── migrations/                # Миграции базы данных
└── 📁 users/                       # Приложение пользователей
    ├── models.py                  # Модели: User, Payment
    ├── views.py                   # Представления
    ├── serializers.py             # Сериализаторы
    ├── urls.py                    # URL маршруты
    └── migrations/                # Миграции
```

## 🔧 Технологический стек

- **Backend:** Django 6.0 + Django REST Framework
- **Аутентификация:** JWT (djangorestframework-simplejwt)
- **База данных:** SQLite (по умолчанию)
- **Документация API:** Swagger (drf-yasg)
- **Валидация:** Встроенные валидаторы Django REST Framework

## 📄 Документация API

После запуска сервера документация доступна по адресам:
- Swagger UI: http://localhost:8000/swagger/
- ReDoc: http://localhost:8000/redoc/

---

**🎉 Проект успешно протестирован и готов к использованию >> README.md*
