# Educational Platform API

Django REST API для образовательной платформы с интеграцией платежной системы Stripe и асинхронными задачами Celery.

## 🚀 Быстрый старт

### 1. Клонирование и настройка окружения
```bash
# Клонировать репозиторий
git clone https://github.com/ZhannaIvanova10/educational-platform-api.git
cd educational-platform-api

# Создать виртуальное окружение
python -m venv venv

# Активировать окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Настроить переменные окружения
cp .env.example .env
# Отредактировать .env файл при необходимости
📚 Функциональность
Пользователи
Регистрация и аутентификация (JWT)

Профиль пользователя

Кастомная модель пользователя

Приветственные письма новым пользователям (Celery task)

Автоматическая блокировка неактивных пользователей (ежедневно в 3:00)

Образовательный контент
Курсы (создание, просмотр, обновление, удаление)

Уроки (привязаны к курсам)

Система подписок на курсы

🎯 Celery задачи
Асинхронная рассылка писем
При обновлении курса автоматически отправляются уведомления всем подписчикам

Проверка времени (не чаще чем раз в 4 часа)

Отправка через Celery worker (асинхронно)

Периодические задачи
Ежедневная проверка неактивных пользователей (в 3:00)

Блокировка пользователей, не заходивших более 30 дней

Уведомление администратора о заблокированных пользователях

Приветственные письма
Автоматическая отправка при регистрации нового пользователя

💳 Платежи (Stripe)
Интеграция с Stripe для обработки платежей

Создание платежных сессий

Обработка вебхуков

История платежей

🔧 Технологии
Backend: Django 5.2, Django REST Framework

Асинхронные задачи: Celery 5.6, Redis

Платежи: Stripe API

Аутентификация: JWT (Simple JWT)

Документация API: DRF Spectacular

База данных: SQLite (разработка), PostgreSQL (продакшн)
📁 Структура проекта
text
materials_project/
├── config/              # Настройки Django и Celery
│   ├── celery.py       # Конфигурация Celery
│   └── settings.py     # Настройки проекта
├── materials/          # Приложение курсов и уроков
│   ├── tasks/         # Celery задачи для курсов
│   │   └── celery_tasks.py  # Задачи рассылки писем
│   ├── models.py      # Модели Course, Lesson, Subscription
│   └── views.py       # ViewSets с интеграцией Celery
├── users/             # Приложение пользователей
│   ├── tasks.py       # Celery задачи пользователей
│   └── models.py      # Кастомная модель User
├── .env.example       # Шаблон переменных окружения
├── requirements.txt   # Зависимости Python
├── manage.py          # Django CLI
└── README.md          # Эта документация
📡 API Endpoints
Аутентификация
POST /api/users/register/ - регистрация
POST /api/users/token/ - получение JWT токена
POST /api/users/token/refresh/ - обновление токена

Курсы
GET /api/courses/ - список курсов
POST /api/courses/ - создание курса
GET /api/courses/{id}/ - детали курса
PUT /api/courses/{id}/ - обновление курса (отправляет уведомления подписчикам)
DELETE /api/courses/{id}/ - удаление курса
POST /api/courses/{id}/subscribe/ - подписка на курс
DELETE /api/courses/{id}/unsubscribe/ - отписка от курса

Уроки
GET /api/lessons/ - список уроков
POST /api/lessons/ - создание урока
GET /api/lessons/{id}/ - детали урока
PUT /api/lessons/{id}/ - обновление урока (отправляет уведомления подписчикам курса)
DELETE /api/lessons/{id}/ - удаление урока

Платежи
POST /api/users/payments/create/ - создание платежной сессии
GET /api/users/payments/ - список платежей
GET /api/users/payments/{id}/ - детали платежа
GET /api/users/payments/check_status/ - проверка статуса
🔒 Безопасность
Все секреты хранятся в .env файле (не в репозитории)

.env.example содержит только примеры значений

GitHub Push Protection включен

JWT токены с ограниченным временем жизни

📄 Лицензия
MIT
